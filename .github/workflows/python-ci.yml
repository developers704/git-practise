name: Python CI

on: [push]

jobs:
  test-and-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Pytest
        run: pip install pytest

      - name: Run Tests
        run: python -m pytest

      - name: Build Docker Image
        run: docker build -t git-practise-python .